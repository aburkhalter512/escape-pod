# Used by `npm run register-commands` (scripts/register-commands.ts) —
# the only thing left reading process.env directly. Local Worker dev
# (`npm run dev:cloudflare`) reads from wrangler.toml's [vars] plus
# `wrangler secret put`-managed secrets instead, not this file.
DISCORD_APPLICATION_ID=
DISCORD_BOT_TOKEN=
